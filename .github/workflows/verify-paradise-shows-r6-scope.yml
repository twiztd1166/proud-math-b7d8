name: Paradise Shows R6 scope verifier

on:
  push:
    branches:
      - release/r6-east-coast-scope
      - hotfix/r6-scope-recursion
      - hotfix/r6-scope-separate-renderer

permissions:
  contents: read

jobs:
  verify-r6-scope:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      API: https://taxlrlfsobtnbasjcnuf.supabase.co/functions/v1/shows-api
      SITE: https://paradise-shows-public.proud-math-b7d8.pages.dev
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Verify R6 scope controller syntax and wiring
        shell: bash
        run: |
          set -euo pipefail
          node --check public/app-annual-scope.js
          node --check public/app-shows.js
          grep -q 'app-annual-scope.js' src/pages/index.astro
          grep -q "paradise-shows-public-v2" public/sw.js
          grep -q "'/app-annual-scope.js'" public/sw.js
          grep -q "ANNUAL_SCOPE_DEFAULT='EAST_COAST_FLORIDA'" public/app-annual-scope.js
          grep -q "planning_category" public/app-annual-scope.js
          grep -q "geographic_region" public/app-annual-scope.js
          grep -q "renderAnnualPlanScoped" public/app-annual-scope.js
          grep -q "typeof window.renderAnnualPlanScoped==='function'?window.renderAnnualPlanScoped():renderAnnualPlan()" public/app-shows.js
          if grep -q "window.renderAnnualPlan=function" public/app-annual-scope.js; then
            echo '::error::Base annual-plan renderer must not be replaced.'
            exit 1
          fi
          echo 'R6 separate-renderer syntax/wiring: PASS'

      - name: Verify R6 classifier and separate renderer
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs=require('fs'),vm=require('vm'),assert=require('assert');
          const source=fs.readFileSync('public/app-annual-scope.js','utf8');
          let baseCalls=0,listenerCalls=0;
          const original=function(){baseCalls++;return '<div>base:'+sandbox.state.annualPlan.rows.map(r=>r.profile_id).join(',')+'</div>'};
          const sandbox={
            state:{annualPlan:{
              rows:[
                {profile_id:'LIFE-011',plan_decision:'PURSUE',event_start:'2027-01-01'},
                {profile_id:'LIFE-017',plan_decision:'WATCH',expected_month:2},
                {profile_id:'HIST-108',plan_decision:'WATCH',conflict_notes:'controlled'},
              ],
              summary:{rows:3,profiles:3,pursue:1,watch:2,research:0,exact:1,expected:1,conflicts:1},
              loaded:true,run:{id:'test'},filter:'ALL'
            },search:''},
            renderAnnualPlan:original,
            render(){},
            esc(v){return String(v??'')},
            document:{addEventListener(){listenerCalls++}},
            console,
          };
          sandbox.window=sandbox;
          vm.createContext(sandbox);
          vm.runInContext(source,sandbox,{filename:'app-annual-scope.js'});
          assert.strictEqual(sandbox.renderAnnualPlan,original,'base renderer must remain untouched');
          assert.equal(typeof sandbox.renderAnnualPlanScoped,'function');
          const t=sandbox.__PARADISE_ANNUAL_SCOPE_TEST__;
          assert(t,'test exports missing');
          const west=['LIFE-017','LIFE-025','LIFE-032','LIFE-044','LIFE-062','LIFE-071','LIFE-099','LIFE-109','LIFE-124','LIFE-172'];
          for(const profile_id of west)assert.equal(t.annualScopeKey({profile_id}),'WEST_COAST_FLORIDA',profile_id);
          assert.equal(t.annualScopeKey({profile_id:'HIST-108'}),'B2B');
          assert.equal(t.annualScopeKey({profile_id:'LIFE-011'}),'EAST_COAST_FLORIDA');
          assert.equal(t.annualScopeKey({profile_id:'X',geographic_region:'CENTRAL_FLORIDA',planning_category:'CONSUMER'}),'CENTRAL_FLORIDA');
          assert.equal(t.annualScopeKey({profile_id:'X',geographic_region:'NORTHERN_FLORIDA',planning_category:'CONSUMER'}),'NORTHERN_FLORIDA');
          assert.equal(t.annualScopeKey({profile_id:'X',geographic_region:'PANHANDLE_FLORIDA',planning_category:'CONSUMER'}),'PANHANDLE_FLORIDA');
          assert.equal(t.annualScopeKey({profile_id:'X',geographic_region:'WEST_COAST_FLORIDA',planning_category:'B2B'}),'B2B');
          const first=sandbox.renderAnnualPlanScoped();
          assert(first.includes('East Coast Florida 1'),first);
          assert(first.includes('West Coast Florida 1'),first);
          assert(first.includes('B2B 1'),first);
          assert(first.includes('base:LIFE-011'),first);
          assert.equal(baseCalls,1,'separate renderer should call base exactly once');
          assert.equal(sandbox.state.annualPlan.rows.length,3,'full row set must be restored');
          assert.equal(listenerCalls,1,'listener installed once');
          vm.runInContext(source,sandbox,{filename:'app-annual-scope.js'});
          assert.strictEqual(sandbox.renderAnnualPlan,original,'base renderer must remain untouched after re-evaluation');
          const second=sandbox.renderAnnualPlanScoped();
          assert(second.includes('base:LIFE-011'),second);
          assert.equal(baseCalls,2,'second evaluation must still call base once');
          assert.equal(listenerCalls,1,'listener must not duplicate');
          console.log('R6 classifier/separate renderer: PASS');
          NODE

      - name: Confirm production remains R5 before promotion
        shell: bash
        run: |
          set -euo pipefail
          curl --retry 5 --retry-delay 2 --retry-all-errors -fsS -o /tmp/annual-plan.json \
            -X POST "$API" -H "Origin: $SITE" -H "Content-Type: application/json" \
            --data '{"action":"annualPlan","year":2027}'
          python - <<'PY'
          import json
          x=json.load(open('/tmp/annual-plan.json'))
          assert x.get('ok') is True, x
          run=x.get('run') or {}
          summary=x.get('summary') or {}
          assert run.get('id')=='0294bac9-5400-4f31-9a13-fc7897b4ddfd', run
          assert run.get('status')=='READY', run
          assert summary.get('rows')==104 and summary.get('profiles')==100, summary
          assert summary.get('pursue')==35 and summary.get('watch')==69, summary
          print('Production R5 pre-promotion control: PASS')
          PY
