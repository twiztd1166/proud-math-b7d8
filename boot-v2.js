const NH=document.getElementById('nHist');
function render(){NL.classList.toggle('on',view==='lookup'||view==='browse');NR.classList.toggle('on',view==='release');NH.classList.toggle('on',view==='history');if(!db||!db.records||db.records.length!==78||db.meta.goCount!==76||db.meta.noGoCount!==2){M.innerHTML='<div class="empty"><h2>Rules could not be loaded</h2><p>Do not use this app for canvassing until it reloads correctly.</p></div>';return}if(view==='history')return history();if(view==='browse')return browse();view==='release'?release():sel?city():lookupHome()}
NL.onclick=()=>setView('lookup');NR.onclick=()=>setView('release');NH.onclick=()=>setView('history');document.getElementById('reset').onclick=home;render();