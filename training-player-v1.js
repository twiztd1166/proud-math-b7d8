const PU_PLAYER_VERSION='2026.08.16-pu-player-v1';
let puPlayerMediaId='';
function puPlayerRoot(){let r=document.getElementById('puPlayerRoot');if(!r){r=document.createElement('div');r.id='puPlayerRoot';r.className='puPlayerRoot';document.body.appendChild(r)}return r}
function puDriveId(url=''){let m=String(url).match(/\/d\/([^/]+)/)||String(url).match(/[?&]id=([^&]+)/);return m?m[1]:''}
function puTime(sec){sec=Math.max(0,Number(sec)||0);let m=Math.floor(sec/60),s=Math.floor(sec%60);return`${m}:${String(s).padStart(2,'0')}`}
function puResumeRead(){try{return JSON.parse(localStorage.puMediaResume||'{}')}catch{return{}}}
function puResumeWrite(v){localStorage.puMediaResume=JSON.stringify(v)}
function puMediaRemember(id,time=0){let x=puResumeRead();x[id]={...(x[id]||{}),time:Number(time)||0,updatedAt:new Date().toISOString()};puResumeWrite(x);localStorage.puLastMedia=id}
function puMediaAuthority(m){if(m?.authority==='PARADISE_APPROVED')return{label:'PARADISE APPROVED',cls:'approved',note:'Paradise-approved training. Current field instructions still control the actual route.'};if(m?.authority==='HISTORICAL')return{label:'HISTORICAL SOURCE',cls:'historical',note:'Historical source training. Learn the useful concept only; Paradise-approved curriculum and current field instructions control.'};return{label:'SOURCE / REFERENCE',cls:'reference',note:'Source/reference training. Paradise-approved curriculum and current field instructions control.'}}
function puPlayerOpen(id){let m=(window.PU_CONTENT?.media||[]).find(x=>x.id===id);if(!m)return;let root=puPlayerRoot();if(puPlayerMediaId===id&&root.innerHTML){root.classList.remove('minimized');return}puPlayerMediaId=id;localStorage.puLastMedia=id;root.className='puPlayerRoot';root.innerHTML=puPlayerMarkup(m);puPlayerWire(m)}
function puPlayerMarkup(m){let stream=String(m.streamUrl||'').trim(),drive=puDriveId(m.url),provider=!stream&&drive,a=puMediaAuthority(m);return`<section class="puPlayerPanel" role="dialog" aria-label="Training player"><div class="puPlayerHead"><div class="puPlayerTitle"><small>${esc(m.trainer||'Paradise University')} · ${m.type==='video'?'VIDEO':'AUDIO'}</small><b>${esc(m.title)}</b><span class="puPlayerAuthority ${a.cls}">${esc(a.label)}</span></div><button id="puPlayerMin" class="puPlayerIcon" aria-label="Minimize player">—</button><button id="puPlayerClose" class="puPlayerIcon" aria-label="Close player">×</button></div><div class="puPlayerBody">${stream?`<div class="puNativeWrap">${m.type==='video'?`<video id="puNativeMedia" playsinline preload="metadata" src="${esc(stream)}"></video>`:`<audio id="puNativeMedia" preload="metadata" src="${esc(stream)}"></audio>`}<div class="puControls"><button id="puBack15">−15</button><button id="puPlayPause" class="puPlayPause">PLAY</button><button id="puFwd30">+30</button><select id="puSpeed" aria-label="Playback speed"><option value="0.75">0.75×</option><option value="1" selected>1×</option><option value="1.25">1.25×</option><option value="1.5">1.5×</option><option value="1.75">1.75×</option><option value="2">2×</option></select></div><div class="puSeek"><input id="puSeek" type="range" min="0" max="100" value="0" step="0.1" aria-label="Media position"><span id="puTime">0:00 / 0:00</span></div></div>`:provider?`<div class="puProviderWrap"><iframe id="puDrivePlayer" src="https://drive.google.com/file/d/${esc(drive)}/preview" allow="autoplay; fullscreen" allowfullscreen loading="eager" title="${esc(m.title)}"></iframe></div>`:`<div class="puProviderMissing">This source does not yet have an in-app playback URL.</div>`}<div class="puPlayerActions">${m.url?`<a href="${esc(m.url)}" target="_blank" rel="noopener">OPEN SOURCE ↗</a>`:''}<button id="puPlayerMin2">MINIMIZE</button></div><div class="puPlayerNote"><b>${esc(a.label)}:</b> ${esc(a.note)}${provider?' Playback is coming from the controlled Drive source. If Drive asks you to sign in, use Open Source.':' Playback position and speed are saved on this device.'}${m.note?`<br><br>${esc(m.note)}`:''}</div></div><button id="puPlayerMini" class="puPlayerMini" aria-label="Open training player"><span>▶</span><span><small>${esc(a.label)}</small><b>${esc(m.title)}</b></span><span>↑</span></button></section>`}
function puPlayerWire(m){let root=puPlayerRoot();let min=()=>root.classList.add('minimized'),open=()=>root.classList.remove('minimized');root.querySelector('#puPlayerMin').onclick=min;root.querySelector('#puPlayerMin2').onclick=min;root.querySelector('#puPlayerMini').onclick=open;root.querySelector('#puPlayerClose').onclick=()=>puPlayerClose();let media=root.querySelector('#puNativeMedia');if(!media)return;let play=root.querySelector('#puPlayPause'),back=root.querySelector('#puBack15'),fwd=root.querySelector('#puFwd30'),speed=root.querySelector('#puSpeed'),seek=root.querySelector('#puSeek'),time=root.querySelector('#puTime'),lastSaved=0;function draw(){let d=Number(media.duration)||0,c=Number(media.currentTime)||0;seek.value=d?String(c/d*100):'0';time.textContent=`${puTime(c)} / ${puTime(d)}`;play.textContent=media.paused?'PLAY':'PAUSE';if(Math.abs(c-lastSaved)>=5){puMediaRemember(m.id,c);lastSaved=c}}media.addEventListener('loadedmetadata',()=>{let saved=puResumeRead()[m.id]?.time||0;if(saved>0&&saved<media.duration-3)media.currentTime=saved;draw()});media.addEventListener('timeupdate',draw);media.addEventListener('play',draw);media.addEventListener('pause',()=>{puMediaRemember(m.id,media.currentTime);draw()});media.addEventListener('ended',()=>{puMediaRemember(m.id,0);draw()});play.onclick=()=>media.paused?media.play():media.pause();back.onclick=()=>media.currentTime=Math.max(0,media.currentTime-15);fwd.onclick=()=>media.currentTime=Math.min(Number(media.duration)||Infinity,media.currentTime+30);speed.onchange=()=>media.playbackRate=Number(speed.value)||1;seek.oninput=()=>{let d=Number(media.duration)||0;if(d)media.currentTime=d*Number(seek.value)/100};try{if('mediaSession'in navigator){navigator.mediaSession.metadata=new MediaMetadata({title:m.title,artist:m.trainer||'Paradise University',album:'Paradise University'});navigator.mediaSession.setActionHandler('play',()=>media.play());navigator.mediaSession.setActionHandler('pause',()=>media.pause());navigator.mediaSession.setActionHandler('seekbackward',()=>media.currentTime=Math.max(0,media.currentTime-15));navigator.mediaSession.setActionHandler('seekforward',()=>media.currentTime=Math.min(Number(media.duration)||Infinity,media.currentTime+30))}}catch{}draw()}
function puPlayerClose(){let root=puPlayerRoot(),media=root.querySelector('#puNativeMedia');if(media)puMediaRemember(puPlayerMediaId,media.currentTime);root.innerHTML='';root.className='puPlayerRoot';puPlayerMediaId=''}
function puBindMediaButtons(scope=document){scope.querySelectorAll('[data-media]').forEach(b=>{b.onclick=()=>puPlayerOpen(b.dataset.media)})}
window.PU_PLAYER_VERSION=PU_PLAYER_VERSION;
window.puPlayerOpen=puPlayerOpen;