function releaseHeader(){return`<div class="head"><div><h2>Daily Release</h2><p>${esc(sel.name)} · ${esc(status(sel).label)}</p></div><button class="back" id="cityBack">City</button></div>`}
function progress(step){let pct=Math.max(0,Math.min(100,(step/5)*100));return`<div class="progressWrap"><div class="progressMeta"><b>${step?`CHECK ${step} OF 5`:'ROUTE SETUP'}</b><span>${step?`${Math.round(pct)}%`:'Before checks'}</span></div><div class="progress"><i style="width:${pct}%"></i></div></div>`}
function bindCityBack(){document.getElementById('cityBack').onclick=()=>setView('lookup')}
function releaseSetup(){M.innerHTML=`${releaseHeader()}${progress(0)}<section class="wizardCard"><div class="wizardEyebrow">ROUTE DETAILS</div><h3>Confirm the route first</h3><p class="wizardHelp">Manager and office are remembered on this device. Exact address / route boundary must be confirmed for every release.</p><div class="field"><label>Manager</label><input id="manager" class="input" value="${esc(form.manager)}" placeholder="Manager name"></div><div class="field"><label>Office / Team</label><input id="office" class="input" value="${esc(form.office)}" placeholder="Office or team"></div><div class="field"><label>Territory / Route</label><input id="route" class="input" value="${esc(form.route)}" placeholder="Neighborhood / route"></div><div class="field"><label>Exact Address / Route Boundary *</label><input id="address" class="input" value="${esc(form.address)}" placeholder="Exact street address or boundary" autocomplete="street-address"></div><div class="field"><label>Legal Jurisdiction / Address Rule</label><div class="input readonly">${esc(sel.jurisdiction)}</div></div><label class="confirm"><input id="confirm" type="checkbox" ${form.confirm?'checked':''}><span>I confirmed the exact address / route boundary matches the legal jurisdiction rule shown.</span></label><button id="continue" class="btn primary wizardNext" ${form.address.trim()&&form.confirm?'':'disabled'}>START 5 CHECKS →</button></section><div class="notice"><b>Fail closed.</b> No DEPLOY result is possible without an exact route/address confirmation and five PASS results.</div>`;bindCityBack();let bind=(id,k,store)=>document.getElementById(id).oninput=e=>{form[k]=e.target.value;if(store)localStorage[store]=e.target.value;document.getElementById('continue').disabled=!(form.address.trim()&&form.confirm)};bind('manager','manager','pcmManager');bind('office','office','pcmOffice');bind('route','route');bind('address','address');document.getElementById('confirm').onchange=e=>{form.confirm=e.target.checked;document.getElementById('continue').disabled=!(form.address.trim()&&form.confirm)};document.getElementById('continue').onclick=()=>{if(!form.address.trim()||!form.confirm){toast('Confirm the exact route/address first.');return}releaseStep=1;release()}}
function releaseCheck(){let x=checks[releaseStep-1],current=form.c[x.key];M.innerHTML=`${releaseHeader()}${progress(releaseStep)}<section class="wizardCard checkScreen"><div class="wizardEyebrow">STEP ${releaseStep} OF 5</div><h3>${esc(x.title)}</h3><p class="wizardHelp">${esc(x.help(sel)||'—')}</p><div class="bigSeg"><button data-v="PASS" class="${current==='PASS'?'PASS':''}"><b>✓ PASS</b><span>Requirement confirmed</span></button><button data-v="STOP" class="${current==='STOP'?'STOP':''}"><b>✕ STOP</b><span>Do not deploy</span></button><button data-v="ESCALATE" class="${current==='ESCALATE'?'ESCALATE':''}"><b>! ESCALATE</b><span>Get the issue resolved first</span></button></div><div class="wizardNav"><button id="prev" class="btn secondary">← BACK</button><span>${releaseStep} / 5</span></div></section>`;bindCityBack();document.querySelectorAll('.bigSeg button').forEach(b=>b.onclick=()=>{form.c[x.key]=b.dataset.v;if(b.dataset.v==='PASS')releaseStep=releaseStep<5?releaseStep+1:6;else releaseStep=6;if(releaseStep===6&&!form.completedAt)form.completedAt=new Date().toISOString();release()});document.getElementById('prev').onclick=()=>{releaseStep=Math.max(0,releaseStep-1);release()}}
