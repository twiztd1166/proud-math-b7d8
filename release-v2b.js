function checkSummary(){return checks.map(x=>`<div class="summaryCheck"><span>${esc(x.title)}</span><b class="state ${esc(form.c[x.key]||'MISSING')}">${esc(form.c[x.key]||'NOT SET')}</b></div>`).join('')}
function releaseFinal(){let d=decision();if(!form.completedAt)form.completedAt=new Date().toISOString();let when=new Date(form.completedAt).toLocaleString();M.innerHTML=`${releaseHeader()}<section class="finalDecision ${d[0]?'yes':'no'}"><small>FINAL DECISION</small><div class="finalSymbol">${d[0]?'✓':'✕'}</div><h3>${d[0]?'DEPLOY':'DO NOT DEPLOY'}</h3><p>${esc(d[1])}</p></section><section class="card finalFacts">${row('Municipality',sel.name,'strong')}${row('Exact route / address',form.address,'strong')}${row('Manager',form.manager||'—')}${row('Office / Team',form.office||'—')}${row('Territory / Route',form.route||'—')}${row('Completed',when)}${row('Snapshot',db.meta.snapshotDate)}</section><section class="card checkSummary">${checkSummary()}</section><div class="field"><label>Notes / Exception / New-Conflict Reference</label><textarea id="notes" class="textarea" placeholder="Optional notes">${esc(form.notes)}</textarea></div><div class="actions finalActions"><button id="copy" class="btn primary">COPY RELEASE RECORD</button><button id="review" class="btn secondary">Review checks</button><button id="newRoute" class="btn secondary">New route</button></div><div class="notice"><b>Recordkeeping note.</b> This version calculates locally and does not yet write the release back to the controlled Google Sheet.</div>`;bindCityBack();document.getElementById('notes').oninput=e=>form.notes=e.target.value;document.getElementById('copy').onclick=copy;document.getElementById('review').onclick=()=>{releaseStep=1;release()};document.getElementById('newRoute').onclick=home}
function release(){if(!sel){M.innerHTML=`<div class="empty"><h2>Select a municipality first</h2><p>Daily Release is tied to a controlled lookup.</p><button class="btn primary" id="go">Go to lookup</button></div>`;document.getElementById('go').onclick=()=>setView('lookup');return}if(sel.release!=='GO'){let s=status(sel);M.innerHTML=`<div class="head"><div><h2>Daily Release</h2><p>${esc(sel.name)}</p></div><button class="back" id="cityBack">Back</button></div><section class="traffic stop"><div class="trafficSymbol">✕</div><div><small>RELEASE BLOCKED</small><h3>${esc(s.label)}</h3><p>${esc(sel.nextAction)}</p></div></section><section class="card"><details open><summary>Why this status?</summary><div class="detail">${esc(sel.why)}</div></details></section>`;bindCityBack();return}if(releaseStep===0)return releaseSetup();if(releaseStep>=1&&releaseStep<=5)return releaseCheck();return releaseFinal()}
async function copy(){let d=decision(),r=sel,t=['PARADISE CANVASS DAILY RELEASE',`Completed: ${form.completedAt?new Date(form.completedAt).toLocaleString():new Date().toLocaleString()}`,`Manager: ${form.manager||'—'}`,`Office / Team: ${form.office||'—'}`,`Route: ${form.route||'—'}`,`Address / Boundary: ${form.address||'—'}`,`City / Area: ${r.name}`,`Legal Jurisdiction: ${r.jurisdiction}`,`Current Release: ${r.release}`,`Time Window: ${form.c.time||'NOT SET'}`,`Signs / Access / Refusal: ${form.c.signs||'NOT SET'}`,`Materials / ROW: ${form.c.materials||'NOT SET'}`,`Permit / ID / Entity: ${form.c.permit||'NOT SET'}`,`Appointment-Only / Script / HSS: ${form.c.appointment||'NOT SET'}`,`Final Decision: ${d[0]?'DEPLOY':'DO NOT DEPLOY'}`,`Decision Reason: ${d[1]}`,`Notes: ${form.notes||'—'}`,`Snapshot: ${db.meta.snapshotDate}`].join('\n');try{await navigator.clipboard.writeText(t);toast('Release record copied.')}catch{toast('Copy failed on this browser.')}}
